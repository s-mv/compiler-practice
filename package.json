{
  "name": "practice",
  "version": "1.0.0",
  "description": "The code in this repository is bound to get very cursed sooner than later.",
  "main": "index.ts",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.5"
  }
}
